# MYGOLD 部署、运行与手机使用

给本机、Kiro 和服务器运维用。仓库：[hemeng88/MYGOLD](https://github.com/hemeng88/MYGOLD)，默认分支 `main`。

线上机器：腾讯云轻量 Ubuntu，目录 **`/opt/mygold`**，公网 IP **`49.232.222.121`**，域名 **`ohmygold.icu`**。
容器：`mygold`（FastAPI，内部 8000）、`mygold-caddy`（80 / 443）。数据库：`/opt/mygold/data/mygold.db`（bind mount，重建容器不丢）。

> 名字还叫 MYGOLD、域名还是 ohmygold.icu，但**黄金相关功能已全部移除**，现在是纯基金工具。改名会牵扯域名和证书，暂未处理。

---

## 1. 工程是什么

一个基金实时估值工具。核心逻辑：**基金公示的季报重仓 × 持仓股当前涨跌幅 = 估算净值涨跌**。

- 收藏基金，填入持有份额和成本价，换算出今日估算盈亏和累计盈亏（金额，不只是百分比）
- 涨幅榜穿透：把榜首基金的重仓汇总，被反复重仓的股票才是当期主线
- 前端 React（Vite），浏览器走相对路径 `/api`；手机用 Safari 加到主屏幕使用

只看数据，不接券商、不下单。

不要提交：`.env`、`data/*.db`、密钥。不要 `git push --force`。

### 数据源

| 用途 | 来源 |
| --- | --- |
| 季报持仓明细 | 天天基金 `fundf10.eastmoney.com/FundArchivesDatas.aspx?type=jjcc`（JSONP 包 HTML，按标签解析） |
| 基金名称与官方净值 | `fundmobapi.eastmoney.com/FundMNewApi/FundMNFInfo` |
| 基金搜索 | `fundsuggest.eastmoney.com/FundSearch/api/FundSearchAPI.ashx`（须按 `CATEGORY==700` 过滤，否则会混进指数） |
| 涨幅榜 | `fund.eastmoney.com/data/rankhandler.aspx` |
| 持仓股行情 | `push2.eastmoney.com` → `push2delay.eastmoney.com` 镜像 → 新浪 `hq.sinajs.cn` 三级兜底 |

持仓股行情**必须保留三级兜底**：push2 请求密了会直接断连，实测丢一批能让覆盖率从 94% 掉到 66%，估算随之失真。新浪只认 A 股和港股。

### 两个容易踩的坑

- **货币基金**：净值接口返回的 `NAV` 是**万份收益**，不是单位净值。货币基金单位净值恒为 1.0000，收益以份额结转。直接拿去乘份额会算出巨额假亏损，代码里按基金类型/名称含「货币」识别后强制按 1.0 计。
- **覆盖度过低**：季报只公示前十大重仓，债券基金可能只公示 0.3% 的股票仓位。按覆盖度放大会得出荒谬数字，所以低于 `fund_min_coverage_pct`（默认 20%）时不给估算，只给说明。

---

## 2. 本机运行（开发）

需要 Python 3.9+、Node 18+。

```bash
cd "/Users/dp/Desktop/for my love"
chmod +x start.sh
./start.sh
```

- 前端：<http://127.0.0.1:5173>（Vite 把 `/api` 代理到 8000）
- 健康检查：<http://127.0.0.1:8000/api/health>
- 本地库：`backend/data/mygold.db`

分开启动：

```bash
python3 -m venv backend/.venv
source backend/.venv/bin/activate
pip install -r backend/requirements.txt
uvicorn app.main:app --app-dir backend --reload --port 8000

cd frontend
npm install
npm run dev
```

改完代码前后端都要验一遍：

```bash
cd frontend && npx tsc --noEmit && npm run build
cd ../backend && ./.venv/bin/python -m compileall -q app
```

---

## 3. 部署到服务器

服务器要有 Docker 和 Compose 插件。防火墙放行 **TCP 80、443**。

### 日常更新（最常用）

国内直连 GitHub 常超时，`update.sh` 走 ghfast 镜像拉 `main` 再重建容器。**它只认 `main` 分支**，功能没合进 main 就不会生效。

```bash
cd /opt/mygold
sudo ./update.sh
```

`data/` 和 `.env` 不会被清掉。数据库新增字段由 `ensure_schema()` 里的 `_ensure_column` 自动 `ALTER TABLE` 补列，不需要手工迁移。

验证部署是否真的生效，最直接的办法是对比前端 bundle 文件名：

```bash
curl -s http://49.232.222.121/ | grep -o 'index-[A-Za-z0-9_-]*\.js'
```

本地对应值：`grep -oE 'index-[A-Za-z0-9_-]+\.js' frontend/dist/index.html | head -1`。两边一致才算部署成功 —— 只看 `/api/health` 返回 200 是不够的，旧容器也会返回 200。

### 第一次在服务器上装

```bash
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker "$USER"

sudo git clone https://github.com/hemeng88/MYGOLD.git /opt/mygold
cd /opt/mygold
sudo chmod +x deploy.sh update.sh
sudo ./deploy.sh
```

`deploy.sh` 会写 `.env` 里的 `MYGOLD_DOMAIN=ohmygold.icu`、生成 `Caddyfile`，然后 `docker compose up -d --build --force-recreate`。前端在 Dockerfile 的多阶段构建里用 `npm ci` 编译，所以 `package.json` 和 `package-lock.json` **必须同步**，否则镜像构建失败（本地 `npm install` 后记得把 lock 一起提交）。

部署完首次打开「涨幅榜」会是空的，定时任务要等当晚 21:30。想立刻要数据就点榜单区域的**刷新**，约 20 秒。

### 本机 rsync 到服务器（需要 SSH 可用）

```bash
chmod +x deploy.sh scripts/publish-to-server.sh
./scripts/publish-to-server.sh root@49.232.222.121 /opt/mygold
```

脚本排除 `data`、`.env`，避免覆盖线上库。**本机 SSH 目前进不去**（只接受密钥认证），实际请用 OrcaTerm 在服务器上执行 `update.sh`。

### 常用运维

```bash
cd /opt/mygold
docker compose ps
docker compose logs -f --tail=100
docker compose restart
```

建议每天备份库：

```bash
mkdir -p /opt/mygold/data/backup
# crontab -e，每天 0:10
10 0 * * * cp /opt/mygold/data/mygold.db /opt/mygold/data/backup/mygold-$(date +\%F).db
```

Kiro 在服务器改完代码后：不要只重启进程，应 `sudo ./deploy.sh` 或 `docker compose up -d --build`，保证镜像和挂载一致。不要删 `data/`。

### 已知问题：HTTPS 域名不通

`https://ohmygold.icu` 的 443 端口能建 TCP 连接，但 TLS 握手被重置（`Connection reset by peer`），基本可以判断是 Caddy 申请证书失败。而 `http://ohmygold.icu` 会被 Caddy 自动 302 跳到坏掉的 https，所以**域名整个不可用**。

目前请用 **<http://49.232.222.121>**。要修的话得看服务器上 `docker compose logs caddy` 里的 ACME 报错。

---

## 4. 手机上使用

**推荐：Safari 打开 <http://49.232.222.121> → 分享 → 添加到主屏幕。** 之后点图标进来，和 App 体感接近。首次访问页面顶部会有添加提示，点「知道了」可永久关闭。

### iOS 原生壳（当前不可用）

`frontend/ios/` 还留着 Capacitor 工程，但**装不进手机**：设备是 iPhone 15 Pro、系统 iOS 27.0 beta，而本机 Xcode 26.6 最高只支持 iOS 26.5，`devicectl` 直接把设备标为 `connected (no DDI)`，Xcode 报「developer disk image could not be mounted」。要用必须装支持 iOS 27 的 Xcode 27。

原生壳加载的是**打包进去的**网页文件（`capacitor.config.ts` 没配 `server.url`），所以每次改前端都得重新 `npm run ios` 再过 Xcode。网页方案没这个问题。

---

## 5. 检查是否成功

| 检查 | 命令或动作 |
| --- | --- |
| 接口活着 | `curl -sS http://49.232.222.121/api/health` |
| 部署到位 | 前端 bundle 文件名与本地 `frontend/dist/index.html` 里一致（见 3.1） |
| 基金估值 | `curl -sS http://49.232.222.121/api/funds`，收藏过基金后应有 `estimate_pct`、`covered_pct` |
| 涨幅榜 | `curl -sS http://49.232.222.121/api/funds/rankings`，`funds` 和 `hot_stocks` 非空 |
| 容器在跑 | `docker compose ps` 里 `mygold`、`mygold-caddy` 为 Up |
| 网页 | 手机浏览器能开基金列表、搜索、填份额 |

---

## 6. 接口速查

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/health` | 健康检查 |
| GET | `/api/funds` | 收藏列表 + 实时估值 + 盈亏 |
| GET | `/api/funds/{code}` | 单只详情，含逐只持仓贡献 |
| GET | `/api/funds/search?q=` | 按代码或名称搜基金 |
| GET | `/api/funds/favorites` | 收藏（含份额、成本价） |
| POST | `/api/funds/favorites` | 新增收藏，body `{"code":"161725"}`，会立刻同步持仓与报价 |
| PUT | `/api/funds/favorites/{code}/position` | 存份额和成本价，body `{"shares":1000,"cost_price":2.5}`；两者传 `null` 表示清空 |
| DELETE | `/api/funds/favorites/{code}` | 取消收藏，连带清理持仓与孤儿报价 |
| POST | `/api/funds/refresh?include_holdings=` | 刷净值与持仓股报价；带 `include_holdings=true` 才重拉季报 |
| GET | `/api/funds/rankings?period=` | 涨幅榜 + 穿透出的领涨方向 |
| POST | `/api/funds/rankings/refresh` | 立刻重算全部周期（约 20 秒） |

`period` 取 `1yzf` 近1月、`3yzf` 近3月、`6yzf` 近6月、`1nzf` 近1年、`jnzf` 今年来（默认）。

### 鉴权

账号存在数据库 `users` 表里，密码只存 **PBKDF2-HMAC-SHA256 加盐哈希**（26 万轮，单次校验约 77ms），不存明文。没有引入 bcrypt/passlib，用的是标准库。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/auth/login` | body `{"username":"...","password":"..."}`，返回令牌 |
| GET | `/api/auth/me` | 拿令牌验一下是否还有效 |

**除 `/api/health` 和 `/api/auth/login` 外，所有 `/api/funds*` 接口都需要登录**，请求带 `Authorization: Bearer <令牌>`，否则 401。`/api/health` 必须保持免鉴权 —— `docker-compose.yml` 的 healthcheck 在打它，加了鉴权容器会一直 unhealthy。

令牌是 HMAC 签名的，服务端不存会话。每次请求除验签外还会查一次账号是否仍存在，所以账号删掉后旧令牌立刻失效。

签名密钥 `MYGOLD_AUTH_SECRET` **默认不写在代码里**，进程启动时随机生成 —— 仓库是公开的，密钥若提交上去别人就能直接伪造令牌，那比密码泄露更糟。代价是后端重启后要重新登录一次。想让令牌跨重启存活：

```bash
python3 -c "import secrets; print('MYGOLD_AUTH_SECRET=' + secrets.token_urlsafe(32))" | sudo tee -a /opt/mygold/.env
```

### 账号怎么来、密码怎么改

**没有注册和改密界面**，这是有意的（单人自用）。

**代码里没有默认密码。** 任何写进仓库的密码都等于公开密码，所以 `bootstrap_password` 默认为空，不配就不建账号 —— 此时登录一直 401，启动日志会打警告告诉你怎么做。

两种建号方式，任选其一。

**一、部署前写进 `.env`**，空库首次启动时自动建号：

```bash
echo 'MYGOLD_BOOTSTRAP_PASSWORD=换成你自己的长密码' | sudo tee -a /opt/mygold/.env
cd /opt/mygold && sudo ./deploy.sh
```

这个逻辑**只在 `users` 表为空时跑**，之后改了密码不会被重启覆盖回去。

**二、用脚本建号或改密码**（已有账号时改密码只能用这个）：

```bash
cd /opt/mygold
docker compose exec mygold python backend/scripts/set_password.py hemeng
# 提示输入两次，不回显。账号不存在则新建，密码至少 6 位。
```

### 这个登录能防住什么，防不住什么

能防：路过的人、爬到 IP 后直接改你的收藏和份额；仓库里没有任何可用凭据；数据库泄露后密码也不会直接暴露（是加盐哈希）。

**防不住**，这两点必须清楚：

1. **站点是明文 HTTP**（`https://ohmygold.icu` 坏着，见 3.4）。密码和令牌在网络上明文传输，同网段的人能直接抓到。这是当前最大的短板，HTTPS 修好之前登录只能挡住顺手点进来的人。
2. 没有登录失败次数限制，弱密码可被在线爆破。**密码取长一点是目前最有效的对策。**

### 部署了但手机上看不到变化？是缓存

`index.html` 引用的 JS 文件名带内容 hash，所以 **index.html 绝不能被缓存**，否则手机会一直加载旧版前端 —— 表现为部署成功了界面却没变，加了登录也不跳登录页。这个坑踩过几次。

后端已经用中间件显式设了缓存头（`backend/app/main.py` 的 `cache_headers`），验证方式：

```bash
curl -sI http://49.232.222.121/ | grep -i cache-control
# 期望 no-cache
curl -sI http://49.232.222.121/assets/<带hash的js> | grep -i cache-control
# 期望 public, max-age=31536000, immutable
```

如果你手机上已经卡在旧版了，需要清一次缓存才能拿到带这个头的新版：主屏幕图标从后台完全划掉再进；不行就 Safari 里设置 → Safari → 高级 → 网站数据，删掉这个站点，或删掉主屏幕图标重新添加。之后就不会再犯了。

### 仓库转私有会破坏部署路径

`update.sh` 默认从 **ghfast 匿名代理** 拉代码（`https://ghfast.top/https://github.com/hemeng88/MYGOLD.git`）。仓库一旦转为私有，匿名通道立刻失效，`sudo ./update.sh` 会拉不到代码。

转私有后想继续用它，需要带凭据。用 GitHub Personal Access Token（勾 `repo` 权限），走 `update.sh` 已有的环境变量口子：

```bash
cd /opt/mygold
sudo UPDATE_REMOTE="https://<你的token>@github.com/hemeng88/MYGOLD.git" ./update.sh
```

注意这样是直连 GitHub，国内可能慢或超时，ghfast 的加速就用不上了。token 别写进仓库里的任何文件。

---

## 7. 定时任务

| 任务 | 频率 | 做什么 |
| --- | --- | --- |
| `fund-quotes` | 每 60 秒（仅 A 股盘中） | 刷收藏基金的持仓股报价，估值靠它 |
| `fund-holdings` | 每天 16:40 | 对一次季报持仓 + 更新官方净值 |
| `fund-rankings` | 每天 21:30 | 拉五个周期涨幅榜并穿透汇总 |

盘中判断在 `backend/app/market_session.py`：9:15–11:35、12:55–15:10。注意 15:00–15:10 这段 `session_label()` 已显示「已收盘」但仍会轮询，是故意留的重叠，用来抓收盘价。

---

## 8. 目录速查

| 路径 | 作用 |
| --- | --- |
| `backend/app/funds/` | 基金数据源、收藏 CRUD、采集落库、涨幅榜 |
| `backend/app/analysis/fund_estimate.py` | 估值与盈亏计算 |
| `backend/app/market_session.py` | A 股交易时段判断 |
| `backend/app/auth.py` | 登录、HMAC 令牌签发与验签 |
| `backend/app/users.py` | 账号表读写、PBKDF2 密码哈希与校验 |
| `backend/scripts/set_password.py` | 改密码 / 建账号（应用里没有入口） |
| `frontend/src/LoginScreen.tsx` | 登录页 |
| `frontend/src/auth.ts` | 令牌本地存取 |
| `frontend/src/FundsPanel.tsx` | 收藏、搜索、份额录入、盈亏合计 |
| `frontend/src/FundRankPanel.tsx` | 涨幅榜与领涨方向 |
| `frontend/ios/` | Capacitor 工程（当前装不进手机，见 4.1） |
| `deploy.sh` | 写 Caddyfile、构建并启动容器 |
| `update.sh` | 服务器拉 `main` + deploy |
| `start.sh` | 本机前后端一起开 |
| `docker-compose.yml` | mygold + caddy |
| `.env` | 仅服务器，含 `MYGOLD_DOMAIN` |

在用的 7 张表：`users`、`fund_favorites`、`fund_holdings`、`fund_stock_quotes`、`fund_navs`、`fund_rank_entries`、`fund_rank_stocks`。

库里还留着 11 张已删功能的旧表：`price_ticks`、`curve_points`、`daily_summaries`、`market_events`、`daily_bars`、`news_flashes`、`gold_lots`、`app_settings`、`stock_quotes`、`stock_bars`、`stock_news`。删功能时只删了模型、没有 drop 表，所以历史数据还在，也不影响运行。真要清掉得手工 `DROP TABLE`，做之前先备份。

`miniprogram/` 目录里的微信小程序调用的 8 个接口**已全部删除**，现在完全不可用，尚未处理。
